<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:id="@+id/menu_profile">
        <item
            android:id="@+id/profile"
            android:title="@string/profile"
            android:icon="@drawable/ic_man"/>
    </group>

    <group android:id="@+id/menu_package">
        <item
            android:id="@+id/packages"
            android:title="الباقات"
            android:icon="@drawable/delivered"/>
    </group>

    <group android:id="@+id/menu_syndicate">
        <item
            android:id="@+id/syndicate"
            android:title="@string/syndicates"
            android:icon="@drawable/guide"/>
    </group>

    <group android:id="@+id/menu_wallet">
        <item
            android:id="@+id/wallet"
            android:title="@string/rewards"
            android:icon="@drawable/wallet"/>
    </group>

    <group android:id="@+id/menu_suggestions">
        <item
            android:id="@+id/suggestions"
            android:title="@string/suggestions"
            android:icon="@drawable/suggestion"/>
    </group>

    <group android:id="@+id/contactus">
        <item
            android:id="@+id/contactus_fragment"
            android:title="@string/contactus_title"
            android:icon="@drawable/ic_bullhorn"/>
    </group>

    <group android:id="@+id/menu_settings">
        <item
            android:id="@+id/settings"
            android:title="@string/setting"
            android:icon="@drawable/settings"/>
    </group>

    <group android:id="@+id/menu_logout">
        <item
            android:id="@+id/logout"
            android:title="@string/logout"
            android:icon="@drawable/logout"/>
    </group>

</menu>